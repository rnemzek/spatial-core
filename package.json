{
  "name": "@nemzilla/spatial-core",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "description": "Decoupled spatial layer engine: renders domain layer definitions onto a map without domain-specific business logic.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.ts"
  },
  "dependencies": {
    "h3-js": "^4.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
